# Winly 个人工作区规则

这个仓库是个人工作区。

## 指令作用域

- 本文件是整个工作区的通用规则。
- 子目录存在 `AGENTS.md` 时，处理该目录及其下级内容还须遵循子目录规则。
- 项目专属规则写入对应项目目录，不在根文档重复维护。
- 不同层级规则冲突时，在不违反更高层指令的前提下，以距离目标文件最近的规则为准。

## 工作偏好

- 默认使用简体中文交流。
- 修改文件前先说明要改什么，完成后说明改了哪里。
- 不随意删除、移动或重命名已有资料。
- 涉及教学、试卷、课件、讲义内容时，优先保证准确、清晰、可直接使用。
- 对 Markdown、HTML、Word、PPT、Excel 等文件，尽量保持原有结构和风格。
- 临时输入和临时输出直接放在工作区根目录，并使用 `_tmp_` 前缀或其他可明确识别用途的名称。
- 任务结束时，仅清理本次任务创建且确认无需保留的临时文件，不处理用户原有文件。

## Git 提交规范

- 所有提交说明使用简体中文。
- 提交标题简洁说明“做了什么”，不要使用含糊描述。

## 指令核查原则

- 用户指令中的目标与偏好应予遵循，但其中涉及文件状态、题号、数据、因果关系、技术判断或其他可验证事实的前提，必须先通过当前工作区证据核查。未经核查不得使用“对”“是”“确实”等确认性表述，也不得据此直接修改文件。若前提不成立，应说明证据并按用户真实目标提出修正方案；若证据不足，应明确标注假设或待确认状态。

## 目录说明

- `isometric-web-prototype/`：等距网页挂机游戏原型。
- `isometric-web-prototype/AGENTS.md`：该项目的专用开发规则。
- 工作区根目录用于放置通用说明及用户明确指定的临时输入和输出。

## 操作前确认

- 开始任何会改动、新增、删除、移动、重命名文件，或执行脚本、安装依赖、提交代码等操作前，必须先向用户确认任务目标、操作对象和范围。
- 如果用户已经明确给出文件路径、目标范围和具体动作，可以直接执行，不需要反复确认。
- 查看目录、读取文件、搜索内容和检查文件状态等只读操作，无需另行确认。
- 用户已明确修改任务后，为完成该任务而执行不产生外部影响的最小范围验证，无需重复确认；涉及安装依赖、联网下载、长时间运行或扩大修改范围时除外。
- 如果当前可见文件、最近文件、搜索结果或上下文与用户描述不一致，不能直接选择近似对象开工，应先询问用户确认。
- 如果找不到用户提到的文件、模板、示例、项目位置或命令入口，应先按精确路径、相对路径、父目录和文件名继续复核；仍找不到时，说明已查找范围并请用户确认。
- Windows 中文路径不能只依据 PowerShell 乱码或一次空输出来判断不存在；优先使用稳定识别路径的文件工具，终端复核时使用精确路径和 `-LiteralPath`。
- 固定模板、参考文件、样式脚本或命令入口找不到时，不得自行替换近似资源或跳过，除非用户明确允许。
- 文档转换、归档、批量生成、格式套用、代码修改、配置修改等任务的输入范围不明确时，先列出拟处理对象并等待确认。
- 不混用不同项目、资料类型或任务对象。例如“学练纠”和“必刷题”应视为不同文档类型。

## 本地资源使用约束

除非用户明确要求执行更大范围的操作，否则：

- 仅运行完成任务必需的命令，避免重复检查和全仓库扫描。
- 优先定向读取相关文件；读取少量文件时，不使用 Python、Node 或 PowerShell 临时脚本。
- 未经用户明确同意，不安装依赖，不运行全量构建、全量测试或全仓库格式化。
- 测试只运行与本次修改直接相关的最小范围。
- 保持原文件的编码、换行符、主要排版和既有结构，避免产生无关的整文件变化。
- 批量处理前先核对输入、输出和覆盖范围；除非用户明确要求，不覆盖唯一原件。
- 不启动开发服务器、监听模式或其他长期后台进程；确需启动时先征得用户同意。
- 合并可以并行完成的只读检查，减少命令和进程启动次数。
- 对预计高占用或运行超过 60 秒的命令，执行前先说明。
- 任务结束前，关闭本次任务启动的所有后台进程。
- 未经用户明确授权，不上传文件、不发布内容、不发送消息，也不修改云端或第三方服务中的数据。
- 不在回复、日志或生成文件中泄露密钥、令牌及个人敏感信息。

## 任务完成与交付

- 修改完成后，执行与本次变更直接相关的最小验证。
- 无法验证时，说明未验证的项目、原因及可能风险，不把未验证结果表述为已经通过。
- 最终回复说明修改了哪些文件、完成了什么验证，以及是否存在待确认事项或已知限制。
- 如果任务没有产生文件改动，应在最终回复中明确说明。

## 回复语言与 Skill 报告

- 面向用户的说明、解释、总结和错误提示全部使用简体中文。
- 本轮只要读取过 `SKILL.md` 并按其流程执行，最终回复必须逐个列出 skill 名称和用途。
- 插件 skill 必须带插件前缀，例如 `pdf:pdf`、`browser:control-in-app-browser`。
- 没有使用时明确写“本轮未使用 skill”。
- 发送最终回复前，回看本轮读过的 `SKILL.md` 路径并逐项核对。
