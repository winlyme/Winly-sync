# 等距网页挂机原型 · 变更日志

> 本文件记录可验证的项目里程碑、验收结论和仍影响当前决策的历史结果，由产品经理对话维护。当前任务状态与待办见 `PROJECT_MANAGEMENT.md`。

## 记录规则

- 只记录已经实际落地并经过产品复核的事实；技术通过但仍需用户目测的内容必须明确标注。
- 已完成任务以里程碑摘要保存，不重复保留研发过程中的逐轮派单文字。
- 已终止并完成清理的路线只保留决策和删除范围，不继续保留生产过程细节。
- 制度建立前的内容只做可验证基线，不追溯无法确认的逐次修改。

## 2026-08-04

### `T-20260804-009` 修正灰岩投射物命中与炉心飞行高度

- **实际变化**：`game.js` 在现有首领伤害事件中按值保存灰岩守门者本次实际受击目标的网格坐标和视觉序列；`render.js` 将灰岩普攻改为主体与动态投射物分层，投射物飞向捕获目标身体区域，目标随后移动、死亡或失效也不会改投其他角色。
- **灰岩派生资产**：新增 64 张 494×210 `attack_body` 和 37 张 64×64 `attack_projectile` RGBA PNG，分别为 1,191,076 和 65,907 字节；原 64 张普攻源帧继续作为来源与哈希依据，不再被运行时请求。主体与投射物按源包围盒复原后 64/64 帧逐像素差异为零，第 23～59 帧 `x < 335` 的固定弹道残留像素为零。
- **炉心表现**：炉心巨像待机、普攻和程序化失败回退的主体统一相对网格接地点上移 24 屏幕像素；正式分支和回退分支均只在原接地点保留一层程序化阴影，网格、碰撞、深度和交战位不变。
- **正式资产总量**：`art/third-party/boss-runtime/` 当前包含 323 张 PNG、6,131,719 字节；原六组 222 张、4,874,736 字节的文件哈希继续与 manifest 一致，无右向、WebP、技能、召唤物或 AOE 资源。
- **产品验收**：独立复核连续两次实际 member 伤害事件分别捕获对应目标、无目标时不递增；验证动态弹体到达捕获身体点、目标后续变化不影响终点、派生组失败只让灰岩回退，以及炉心正式/回退分支精确 `-24px` 和地面单阴影。派生序列聚合 SHA-256、画布、连续命名、manifest version 4 均一致；11 个 JavaScript 语法通过，页面 11 个本地引用存在且无网络请求。
- **不变范围**：未修改 `combat.js`、`state.js`、`data.js`、地图、首领和角色数值、攻击间隔、受击次数、掉落、存档、蕾琳资产或技能状态；岩冠领主不受影响。
- **人工目测**：灰岩投射物起点、抛物线和命中特效读感，炉心悬空高度及单阴影关系，两档速度下的同步节奏，以及人为制造素材失败后的局部回退仍由用户打开 `index.html` 确认。

### `T-20260804-008` 接入蕾琳场景过渡、营地点地移动与独立速度时钟

- **实际变化**：`core.js`、`state.js`、`world.js`、`render.js`、`ui.js`、`game.js` 增加显式场景过渡阶段、营地 Canvas 主键点地寻路，以及模拟、移动、视觉和镜头四类用途明确的时间增量；`1×` 保持动画、战斗和计时原速，仅将路径移动提高到原基础 `2×`，`2×` 再相对 `1×` 统一翻倍。
- **流程表现**：初次加载及失败、通关、异常路线回营先播放复活再进入稳定营地；挑战按“营地升级 → 副本入口升级 → 自动寻路”执行；失败保留完整死亡序列后离场，只有完整通关播放副本内升级离场，普通首领胜利不触发离场动作。
- **营地交互**：稳定营地主键点击可达地块会覆盖自动漫游路线，抵达后恢复随机等待与自动漫游；活动区外、阻挡、不可达及过渡阶段点击安全忽略。复活与每次抵达后统一等待 4～6 模拟秒，`2×` 下可见为 2～3 秒。
- **正式资产**：`art/third-party/raelyn-runtime/scene_transition/` 新增左右各 24 张、共 48 张 192×156 RGBA PNG，合计 308,567 字节；运行帧逐帧 SHA-256 与用户指定 `levelup` 源一致，且源 `levelup`、`respawn` 两组逐字节相同，因此只保留一套物理帧，由两个逻辑语义共享。蕾琳运行包现共 306 张 PNG、1,464,232 字节。
- **产品验收**：素材帧数、尺寸、格式、哈希、manifest 与运行常量一致；独立复核初次复活、点地合法/非法门禁、双段入场、失败回营、随机等待和两档时钟；11 个 JavaScript 语法通过，页面 11 个本地引用存在且无网络引用。研发报告中的难度、13 段路线、掉落、换装冻结及版本 1/2 存档回归结果一并核对，无返修项。
- **不变范围**：未修改 `data.js`、`combat.js`、`save.js`、地图、首领资产、角色数值、掉落和存档格式；残暴斩击、坚不可摧、激发恐惧和墓碑仍未接入。
- **人工目测**：升级/复活共享帧的语义观感、方向与脚底锚点、失败末帧和通关离场衔接、自动移动改道，以及 `1×/2×` 的真实动作与移动节奏仍由用户打开 `index.html` 确认。

### `T-20260802-007` 接入三名首领待机与普通攻击素材

- **实际变化**：`render.js` 为灰岩守门者、炉心巨像和岩冠领主分别预加载左向待机与普通攻击序列；未攻击时以 30 FPS 循环待机，现有 `attackPulse` 触发后完整播放一次普攻，过长序列只等比加速到下一次攻击前结束，随后从待机首帧重启。
- **正式资产**：`art/third-party/boss-runtime/` 收录用户指定源目录的 222 张左向 RGBA PNG，共 4,874,736 字节；待机为 24/19/40 帧，普攻为 64/27/48 帧。灰岩普攻保留 494×210 完整投射物画布，炉心普攻保留武器与火焰层，未接入右向帧、技能、召唤物或 WebP。
- **表现边界**：六组使用独立固定锚点与缩放；正式 PNG 分支不叠加程序化地面阴影，受击提亮保留；任一动作组加载失败或画布异常时只让对应首领回退到原程序化形象。
- **产品验收**：222/222 运行 PNG 与指定源目录文件名和 SHA-256 一致；帧数、连续命名、画布、锚点、缩放、manifest version 3 和 `render.js` 常量一致；11 个 JavaScript 语法通过，页面 11 个本地引用存在且无网络引用。首次提交因灰岩投射物/后20帧及炉心武器火焰层缺失被退回，返修后复验通过。
- **不变范围**：首领名称、ID、数值、等级链、房间、交战位、路线、掉落、存档结构和技能状态均未改变。
- **人工目测**：真实浏览器中的待机与普攻节奏、投射物观感、比例、脚底稳定、无双重阴影、失败回退及 `1×/2×` 衔接仍由用户打开 `index.html` 确认。

## 2026-08-02

### `T-20260802-005` 确认蕾琳正式路线并清理原创旧人物资产

- **产品决策**：用户确认蕾琳为项目唯一正式角色，原创旧人物路线永久终止；项目名称、角色状态、界面、技术说明和后续任务均须统一到蕾琳。
- **规则变化**：`AGENTS.md` 改为同时承载强制项目指令和稳定开发原则，并明确当前不另拆第四份规则文件；删除待决人物路线和历史资产保留要求，新增正式角色身份、通用回退和旧引用清零规则。
- **资产清理**：删除原创人物风格板、人物概念、方向参考、运行帧、动作诊断、失败审计、生成工具，以及无法从人物内容中拆开的混合预览、manifest 和说明，共 259 个文件、35,370,426 字节（约 33.73 MiB）。
- **保留资产**：蕾琳正式运行帧与说明、地图资料、两张纯首领概念图、首领初版/R1 方向样片、三名首领动作和岩冠领主独立特效，共 329 个文件、6,591,720 字节（约 6.29 MiB）。
- **影响文件**：`AGENTS.md`、`PROJECT_MANAGEMENT.md`、`CHANGELOG.md`；删除 `art/` 下已终止人物路线的相关文件和目录。
- **验证结果**：所有删除目标均先解析为当前项目 `art/` 下的绝对路径；删除后没有残留带废弃人物名称的文件或目录；蕾琳 258 张运行 PNG、三名首领资产和地图资料均保留。
- **遗留事项**：当前没有 Git，本次删除不能通过项目版本历史恢复；身份文字已由 `T-20260802-006` 完成统一。

### `T-20260802-006` 统一蕾琳角色身份并清零旧引用

- **实际变化**：页面默认文案、动态整备摘要、共享角色状态、地图资料和蕾琳运行说明统一使用“蕾琳”；内部角色 ID 改为 `warrior_raelyn`；地图 SVG/PNG 从同一生成器重建；README/manifest 同步无额外人物阴影、通用回退阴影和 `2×` 动画时钟规则。
- **影响文件**：`index.html`、`state.js`、`ui.js`、地图生成器/README/SVG/PNG、蕾琳 README/manifest，共 9 个文件；未修改玩法、数值、地图数据、存档模块、蕾琳 PNG或首领资产。
- **产品验收**：全项目废弃名称、旧 ID 和旧目录路径搜索为零；11 个 JavaScript 语法通过；页面 11 个本地引用存在且无网络请求；manifest 与 258 张 192×156 PNG 对账一致；地图 PNG 为 3700×3700；版本 1/2 存档键和结构保持不变。
- **人工目测**：蕾琳动画、比例、锚点、遮挡、两档速度和人为制造加载失败时的回退表现继续由用户本地确认。

### `T-20260802-004` 新产品经理接手、旧项目梳理与治理记录精简

- 重新确认项目根目录、离线运行链路、玩法闭环、资产分类、开放任务和风险。
- 将项目台账和变更日志从逐轮过程记录压缩为当前事实与里程碑，移除失效对话、错误路径和重复需求展开。
- 15 个当时存在的业务与美术工具 JavaScript 语法通过，页面 11 个本地引用全部存在；营地、副本路线和难度边界通过静态复核。

### `T-20260802-001`、`T-20260802-002` 蕾琳阴影与 `2×` 动画修正

- 蕾琳 PNG 绘制分支不再额外绘制程序化人物阴影；素材失败时的通用程序化回退阴影保留。
- 人物动画使用独立视觉时钟；`2×` 对待机、行走、普通攻击和死亡统一应用两倍动画推进。
- 代码与静态边界通过复核，仍等待用户在真实画面中确认阴影和两档动感。

## 2026-08-01

### `T-20260801-001` 允许用户确认来源的第三方原始素材正式接入

- 项目规则改为允许用户明确指定并确认来源的第三方原始游戏素材作为正式资产，任务需记录来源、范围、适配方式和替换对象。
- 第三方素材在发布到当前原型之外前，仍需按实际范围复核授权和权利条件。

### `T-20260801-002` 接入蕾琳运行素材

- 从用户指定来源接入蕾琳 `idle`、`walk`、`melee_attack`、`death` 四组动作，左右两向共 258 张 192×156 RGBA PNG。
- `render.js` 依据左右朝向加载帧，使用固定锚点和缩放；加载失败、尺寸异常或帧未就绪时回退到通用程序化人物。
- 素材清单、尺寸、路径和代码语法通过技术复核；用户于 2026-08-02 正式确认继续使用蕾琳。

### `T-20260801-003` 重设速度基线

- 删除 `0.5×`，页面只保留 `1×`、`2×`。
- 新 `1×` 使用提高后的玩法基线；新 `2×` 再按两倍推进玩法和人物动画时间。
- 整体节奏仍等待用户目测。

## 2026-07-18

### 地图、交战和场景里程碑

- `T-20260718-001`：三场首领战改为显式交战数据。守门者南向、角色从南侧朝北；巨像西向、角色从西侧朝东；领主南向、角色从南侧朝北。新增同源副本俯视 PNG、SVG、生成器和说明。
- `T-20260718-003`：程序化场景增加稳定物件层级、可见且碰撞一致的障碍和确定性偏好寻路；`zigzag_route` 调整到 `(31,55)`，`main_fork` 调整到 `(42,27)`；完整路线和第二首领后折返通过技术复核与用户目测。
- `T-20260718-004`：营地扩展为固定镜头下看不到边缘的开放场景，中央活动区之外增加非对称自然群落；删除常驻传送门，副本入口改为实体门洞。技术边界通过，真实画面仍待用户目测。

### 已终止原创人物生产

- `T-20260718-002` 曾进行原创人物透明逐帧试制，但没有形成获准接入的完整动作包。
- 用户于 2026-08-02 选择蕾琳正式路线后，该任务取消，相关人物资产和生产记录由 `T-20260802-005` 完成清理。
- 三名首领固定方向动作和岩冠领主独立特效属于独立成果，继续保留。

## 2026-07-17

### 首版玩法与界面闭环

- `T-20260717-001`：桌面整备栏移到 Canvas 右侧，窄屏移到画布下方；副本改为 `entranceRoomId`/`exits` 驱动；异常出口安全回营；难度封顶 99；版本 1/2 存档继续兼容并清洗异常字段。
- `T-20260717-004`：战斗阶段保持战利品详情开合状态但冻结操作；新掉落重新进入待处理队列；单件丢弃取消确认，清空背包保留确认；移除暂停入口和非必要状态说明。
- `T-20260717-005`：继续精简界面说明；当前装备改为只读展示，取消直接卸下，换装后旧装备回包。
- 上述任务通过 JavaScript 语法、本地引用、难度边界、路线异常、掉落防重、背包换装和存档清洗复核。

### 项目治理机制

- `T-20260717-002`、`T-20260717-003`：建立产品经理、研发对话和子代理职责；三份治理文档改为产品经理独占维护；重叠任务必须排队。
- `T-20260717-006`：建立异步派单和研发主动交付机制；产品经理不陪跑研发，收到完成报告后再独立验收。

### 连续副本大地图与首领资产

- `T-20260717-008`：副本扩展为 70×88 连续大地图，统一右上北、右下东、左下南、左上西；三名首领在副本开始时预置；镜头全图跟随；第二首领后可合法返回同一分叉再前往最终厅。
- `T-20260717-009`、`T-20260717-010`：完成三名原创人形首领概念和修订，分别定位为灰岩守门者、炉心巨像和岩冠领主。
- `T-20260717-011`：完成首领透明运行样片；三名首领固定方向和岩冠领主黑曜石环/秘火独立层通过样片验收，尚未接入运行时。

## 制度建立前的可验证基线

### 2026-07-16 等距营地与副本原型

- 已具备等距 Canvas 地图、营地障碍、单角色自动寻路、遮挡排序和速度控制。
- 已扩展为三个连续首领房、自动战斗、装备掉落、背包换装和浏览器本地存档。
- 已拆分为核心、数据、背包、状态、世界、战斗、存档、绘制、界面和编排脚本。
- 本条只作为接管时可验证基线，不代表此前每一次改动的完整记录。
